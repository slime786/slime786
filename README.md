# V9.3.1 — Arcade Naming Polish

Small consistency update to V9.3.

Changes:
- top navigation now uses **Arcade** instead of the old singular **Game**
- any matching navigation references to the game section are normalized to **Arcade**
- the arcade section has a clearer accessibility label
- the existing `#game` anchor is intentionally preserved so old links/bookmarks do not break
- MOHEEN // ARCADE, Space Boost 2.0, Orbital Defender, Signal Decode, XP, scores and sound controls are unchanged

Upload every file in this ZIP to the root of `main`, replacing matching files.
