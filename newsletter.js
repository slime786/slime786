(()=>{const form=document.getElementById('newsletter-form'),status=document.getElementById('newsletter-status'),stories=document.getElementById('finance-stories');fetch('/slime786/newsletter-latest.json',{cache:'no-store'}).then(r=>r.ok?r.json():Promise.reject()).then(d=>{stories.innerHTML=(d.stories||[]).map(s=>`<article class="newsletter-story"><p class="eyebrow">${esc(s.source||'SOURCE')}</p><h3><a href="${url(s.link)}" target="_blank" rel="noopener noreferrer">${esc(s.title)}</a></h3><p>${esc(s.summary||'')}</p></article>`).join('')||'<p>The next edition is being prepared.</p>'}).catch(()=>stories.innerHTML='<p>The next edition is being prepared.</p>');function esc(v){const d=document.createElement('div');d.textContent=String(v||'');return d.innerHTML}function url(v){try{const u=new URL(v);return /^https?:$/.test(u.protocol)?u.href:'#'}catch{return '#'}}form?.addEventListener('submit',async e=>{e.preventDefault();const endpoint=form.dataset.subscribeEndpoint;if(!endpoint){status.textContent='Email signup is being prepared. No details were submitted yet — please check back soon.';return}const btn=form.querySelector('button');btn.disabled=true;status.textContent='Joining…';try{const r=await fetch(endpoint,{method:'POST',body:new FormData(form),headers:{Accept:'application/json'}});if(!r.ok)throw new Error();form.reset();status.textContent='You’re in — check your inbox if confirmation is required.'}catch{status.textContent='Could not subscribe right now. Please try again.'}finally{btn.disabled=false}})})();